'use strict';

describe('myApp.forcedirected module', function() {

  beforeEach(module('myApp.forcedirected'));

  describe('forcedirected controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var forcedirectedCtrl = $controller('ForcedirectedCtrl');
      expect(forcedirectedCtrl).toBeDefined();
    }));

  });
});