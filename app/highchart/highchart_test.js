'use strict';

describe('myApp.highchart module', function() {

  beforeEach(module('myApp.highchart'));

  describe('highchart controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var highchartCtrl = $controller('HighchartCtrl');
      expect(highchartCtrl).toBeDefined();
    }));

  });
});